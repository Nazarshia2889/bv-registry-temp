name = "vg"
version = "1.60.0"
channels = [
    "https://conda.anaconda.org/bioconda",
    "https://conda.anaconda.org/conda-forge",
]
packages = ["vg ==1.60.0"]
platform = "linux/amd64"

[entrypoint]
command = "/opt/conda/bin/vg"
