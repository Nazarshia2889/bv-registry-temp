name = "pigz"
version = "2.8"
channels = [
    "https://conda.anaconda.org/bioconda",
    "https://conda.anaconda.org/conda-forge",
]
packages = ["pigz ==2.8"]
platform = "linux/amd64"

[entrypoint]
command = "/opt/conda/bin/pigz"
