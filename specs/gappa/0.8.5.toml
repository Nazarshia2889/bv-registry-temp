name = "gappa"
version = "0.8.5"
channels = [
    "https://conda.anaconda.org/bioconda",
    "https://conda.anaconda.org/conda-forge",
]
packages = ["gappa ==0.8.5"]
platform = "linux/amd64"

[entrypoint]
command = "/opt/conda/bin/gappa"
