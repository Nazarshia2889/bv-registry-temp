name = "deepvariant"
version = "1.10.0"
channels = [
    "https://conda.anaconda.org/bioconda",
    "https://conda.anaconda.org/conda-forge",
]
packages = ["deepvariant ==1.10.0"]
platform = "linux/amd64"

[entrypoint]
command = "/opt/conda/bin/run_deepvariant"
